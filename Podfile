# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'edadil' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for edadil
  pod 'DTTableViewManager', '~> 6.0.0-beta.3'
  pod 'SnapKit', '~> 4.0.0'
  pod 'MagicalRecord'
  pod 'Alamofire'
  target 'edadilTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'edadilUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
